// sliderbtn => unchecked, checked => {
    unchecked => canvas
    checked => textBox
}
// gaming => unchecked, checked

// sliderBtn => unchecked / gaming => unchecked

// sliderBtn =>  checked / gaming => unchecked

// gaming => checked